{
  "name": "svc-inventory",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "svc-inventory/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"]
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "svc-inventory:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "svc-inventory:build:development"
        },
        "production": {
          "buildTarget": "svc-inventory:build:production"
        }
      }
    }
  },
  "tags": []
}
