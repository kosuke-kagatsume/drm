{
  "name": "svc-booking",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "svc-booking/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"]
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "svc-booking:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "svc-booking:build:development"
        },
        "production": {
          "buildTarget": "svc-booking:build:production"
        }
      }
    }
  },
  "tags": []
}
